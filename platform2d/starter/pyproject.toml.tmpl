[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "my-platform2d-game"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["platform2d>=0.23,<0.24"]

[tool.setuptools.packages.find]
include = ["mygame*"]
