[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "my-platform2d-game"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["platform2d>=0.24,<0.25"]

[tool.setuptools.packages.find]
include = ["mygame*"]

[tool.setuptools.package-data]
"mygame" = ["*.json", "assets/*"]
